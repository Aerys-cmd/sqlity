namespace Sqlity.EFCore;

public static class EfCoreProviderStatus
{
    public const string CurrentMilestone = "Planned after the ADO.NET provider exposes stable command execution and schema metadata for EF Core integration.";
}
