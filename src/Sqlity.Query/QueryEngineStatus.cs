namespace Sqlity.Query;

public static class QueryEngineStatus
{
    public const string CurrentMilestone = "MVP query execution is available for CREATE TABLE, INSERT, and SELECT over the single-page storage engine.";
}
